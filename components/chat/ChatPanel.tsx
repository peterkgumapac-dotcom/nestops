'use client'

interface Props {
  open: boolean
  onClose: () => void
  currentUserId: string
}

// TODO: Step 19 implementation — 340×520px slide-up panel
export function ChatPanel({ open, onClose, currentUserId }: Props) {
  return null
}
